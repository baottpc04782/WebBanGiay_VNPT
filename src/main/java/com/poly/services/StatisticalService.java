package com.poly.services;

import java.util.List;
import java.util.Optional;

import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

import com.poly.entity.Orders;

public interface StatisticalService {

	/*
	 * public List<Orders> findAll() ; //in ra lưu vào danh sách
	 * 
	 * public Page<Orders> findAll(Pageable pageable) ; //phân trang
	 * 
	 * public Optional<Orders> findById(Integer id) ; //tìm kiếm theo id
	 * 
	 * public Orders create(Orders orders) ; //thêm danh sách
	 * 
	 * public Orders update(Orders orders) ; //sửa danh sách
	 * 
	 * public void delete(Orders orders) ;
	 * 
	 * Page<Orders> search(String Fullname, Pageable pageable);
	 * 
	 * 
	 * List<Orders> findByYear(int year);
	 * 
	 * Orders findMaxQuantityAndTotalByYear(int year);
	 */  
}
